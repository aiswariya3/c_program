#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the values of a,b:");
    scanf("%d%d",&a,&b);
    if(a==b)
    printf("both are equal");
    else
    printf("both are not equal");
    return 0;
}
